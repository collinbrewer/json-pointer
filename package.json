{
  "name": "@collinbrewer/json-pointer",
  "version": "0.1.1",
  "description": "A utility for working with RFC 6901 pointers, and a bit more.",
  "main": "src/json-pointer.js",
  "scripts": {
    "test": "mocha src/**/*.test.js",
    "test:watch": "npm run test -- --watch",
    "test:cov": "istanbul cover -x *.test.js --report lcov --report json --report text --report html _mocha -- src/**/*.test.js",
    "pretest:codeclimate": "npm run test:cov",
    "test:codeclimate": "codeclimate-test-reporter < ./coverage/lcov.info",
    "autopublish": "publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/collinbrewer/json-pointer.git"
  },
  "author": "Collin Brewer <collin@brewer-collective.com>",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "codeclimate-test-reporter": "^0.4.0",
    "eslint": "^2.13.1",
    "eslint-config-happiness": "^5.3.1",
    "eslint-plugin-promise": "^1.3.2",
    "eslint-plugin-standard": "^1.3.3",
    "istanbul": "^0.4.4",
    "mocha": "^2.2.5",
    "publish": "^0.6.0"
  },
  "dependencies": {}
}
